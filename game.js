const c=document.getElementById('game'),x=c.getContext('2d');x.imageSmoothingEnabled=false;
const keys={};addEventListener('keydown',e=>{keys[e.key.toLowerCase()]=true;if(['arrowup','arrowdown','arrowleft','arrowright',' '].includes(e.key.toLowerCase()))e.preventDefault()});addEventListener('keyup',e=>keys[e.key.toLowerCase()]=false);
const W=1200,H=700,p={x:600,y:350,s:150,dir:'down',frame:0};let t=0;
const trees=[];for(let i=0;i<90;i++){let a=Math.random()*W,b=Math.random()*H;if(Math.hypot(a-600,b-350)>190)trees.push({x:a,y:b,r:18+Math.random()*12})}
function rect(px,py,w,h,col){x.fillStyle=col;x.fillRect(Math.floor(px),Math.floor(py),w,h)}
function tree(q){rect(q.x-4,q.y+10,8,18,'#4a2c24');rect(q.x-13,q.y,26,19,'#183f2c');rect(q.x-9,q.y-9,18,18,'#24543a');rect(q.x-4,q.y-14,8,8,'#36734a');}
function player(){let bob=p.frame?1:0;rect(p.x-6,p.y+8+bob,12,12,'#34233a');rect(p.x-7,p.y-4+bob,14,14,'#b84f62');rect(p.x-6,p.y-13+bob,12,10,'#f1d0aa');rect(p.x-9,p.y-16+bob,5,7,'#f1d0aa');rect(p.x+4,p.y-16+bob,5,7,'#f1d0aa');rect(p.x-4,p.y-11+bob,2,2,'#20202a');rect(p.x+2,p.y-11+bob,2,2,'#20202a');}
function draw(){x.clearRect(0,0,c.width,c.height);let sx=p.x-160,sy=p.y-90;x.save();x.translate(-sx,-sy);rect(0,0,W,H,'#315b3b');for(let i=0;i<500;i++){let gx=(i*83)%W,gy=(i*47)%H;rect(gx,gy,1,1,i%3?'#3d6840':'#477449')}x.fillStyle='#8b7957';x.fillRect(80,310,1040,46);x.fillRect(520,90,50,520);x.fillStyle='#5e88a0';x.fillRect(0,535,1200,80);x.fillStyle='#78a9bb';x.fillRect(0,535,1200,3);trees.sort((a,b)=>a.y-b.y).forEach(tree);rect(525,290,150,105,'#776044');rect(535,300,130,90,'#936f4b');rect(588,345,26,45,'#4b2d23');rect(548,315,22,18,'#d8ad63');rect(620,315,22,18,'#d8ad63');player();x.restore();
// UI
rect(7,7,105,28,'#111827');rect(11,11,97,5,'#3b202a');rect(11,11,97*Math.max(0,1),5,'#cf5a68');x.fillStyle='#fff';x.font='7px monospace';x.fillText('MOONWOOD  •  HP 100',12,28);x.fillText('WASD / ARROWS  MOVE',7,174);}
let last=performance.now();function loop(now){let dt=Math.min(.033,(now-last)/1000);last=now;let dx=(keys.d||keys.arrowright?1:0)-(keys.a||keys.arrowleft?1:0),dy=(keys.s||keys.arrowdown?1:0)-(keys.w||keys.arrowup?1:0);if(dx||dy){let n=Math.hypot(dx,dy);p.x+=dx/n*p.s*dt;p.y+=dy/n*p.s*dt;p.dir=Math.abs(dx)>Math.abs(dy)?(dx>0?'right':'left'):(dy>0?'down':'up');p.frame=(p.frame+dt*8)%2|0}p.x=Math.max(20,Math.min(W-20,p.x));p.y=Math.max(30,Math.min(H-30,p.y));draw();requestAnimationFrame(loop)}requestAnimationFrame(loop);